/**
 * Interface defining style props that components should accept if they support custom styling
 */
export interface Styles {
    /**
     * The background color to use for this element
     */
    backgroundColor: String;
}

